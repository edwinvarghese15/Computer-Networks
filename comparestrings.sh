echo "Enter the first string:"
read strl
echo "Enter the second string:"
read strm
if [ $strl == $strm ]
then
echo "The first string "$strl "and second string" $strm "are same"
else
echo "The first string "$strl "and second string" $strm "are not same"
fi
